================================================================================
  MOVED — use my dotfiles repo instead of this one.

  This Vim config now lives in https://github.com/dnzhng/dotfiles under vim/.
  Set it up there with:  vim/install.sh
  This repository is kept for history only and is no longer maintained.
================================================================================

Vim config files

## Color scheme and Profile
When using iTerm2, create a profile named `Ocean`.
Add the provided itermcolor profile.

## Init vim packages
after cloning into `~/.vim` run
```
$ git submodule init
$ git submodule update
```
